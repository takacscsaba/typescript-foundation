'use strict';

const 名前: string = 'タカーチ・チャバ';
const 年: number = 23;
const 身長: number = 180;
const 既婚: boolean = false;

console.log(`私の名前は${名前}です。`);
console.log(`私は${年}年です。`);
console.log(`私の身長は${身長}ｃｍです。`);
if (既婚 === false) {
    console.log(`私は結婚していません。`);
} else {
    console.log(`私は結婚しています。`);
}